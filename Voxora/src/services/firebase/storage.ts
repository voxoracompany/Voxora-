// ── V5.3 Firebase Storage — Placeholder ───────────────────────────────────────
// Cloud file storage for avatars, attachments, and exports.
// Full implementation: install firebase/storage and add methods here.
//
//  import { getStorage, ref, uploadBytes, getDownloadURL } from "firebase/storage";
//  import { getFirebaseApp } from "./firebase";
//
//  export function getFirebaseStorage() { return getStorage(getFirebaseApp()); }
//  export async function uploadFile(path: string, file: File): Promise<string> { ... }

export function getStoragePlaceholder(): null {
  return null;
}
